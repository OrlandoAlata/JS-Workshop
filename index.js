console.log("Hello from indexedDB.js!");
alert("Javascript is linked!");

let code1;
code1 = 7 + 3;

let code2 = 40 + (40 % 2);
let code3 = (45 % 6) + 36;

const message1 = "The vault has been secured. The combination is..";

const codeA = "10-40-39";

const codeB = `${code1}-${code2}-${code3};`;

console.log(message1, codeA, codeB);
